apiVersion: v1
kind: Service
metadata:
  name: {{ include "nats-agent.fullname" . }}
  labels:
    {{- include "nats-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nats-agent.selectorLabels" . | nindent 4 }}
